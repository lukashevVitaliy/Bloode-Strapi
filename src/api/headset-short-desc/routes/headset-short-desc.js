'use strict';

/**
 * headset-short-desc router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::headset-short-desc.headset-short-desc');
