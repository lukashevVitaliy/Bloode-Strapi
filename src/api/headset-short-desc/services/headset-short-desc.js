'use strict';

/**
 * headset-short-desc service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::headset-short-desc.headset-short-desc');
