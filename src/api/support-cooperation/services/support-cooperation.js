'use strict';

/**
 * support-cooperation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::support-cooperation.support-cooperation');
