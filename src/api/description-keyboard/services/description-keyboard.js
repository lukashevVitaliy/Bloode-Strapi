'use strict';

/**
 * description-keyboard service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::description-keyboard.description-keyboard');
