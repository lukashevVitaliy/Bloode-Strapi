'use strict';

/**
 * description-keyboard router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::description-keyboard.description-keyboard');
