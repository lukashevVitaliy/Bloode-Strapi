'use strict';

/**
 * keyboard-short-desc router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::keyboard-short-desc.keyboard-short-desc');
