'use strict';

/**
 * keyboard-short-desc service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::keyboard-short-desc.keyboard-short-desc');
