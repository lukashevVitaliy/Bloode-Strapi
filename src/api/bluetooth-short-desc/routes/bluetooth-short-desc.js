'use strict';

/**
 * bluetooth-short-desc router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::bluetooth-short-desc.bluetooth-short-desc');
