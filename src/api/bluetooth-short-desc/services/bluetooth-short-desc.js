'use strict';

/**
 * bluetooth-short-desc service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::bluetooth-short-desc.bluetooth-short-desc');
